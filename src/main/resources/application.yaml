server:
  port: 8888

spring:
  application:
    name: microservices-config

  cloud:
    config:
      server:
        git:
          uri: https://github.com/vjoyaroj/bank-config-repo
          default-label: main
